#!/usr/bin/env ruby
$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)
require 'vocab_tester'

test = VocabTester::Test.new(STDOUT)
test.start
while !test.words.empty? || !test.queue.empty?
	input = gets.chomp
	test.reply(input)
end

